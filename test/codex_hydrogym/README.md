# codex_hydrogym tests

CPU regression tests for work labelled `codex_hydrogym`.

Run from the repository root:

```bash
MPLBACKEND=Agg MPLCONFIGDIR=/tmp/codex_hydrogym-mpl \
  .venv/bin/python -m pytest test/codex_hydrogym -v
```

