# Re=100 Gate 0 v3 protocol — frozen terminal report

**Review status:** Frozen and executed once. All 360 trajectories completed, but one locked temporal effect-equivalence predicate failed; Gate 0 v3 is terminally failed and PPO is not authorized.  
**Scientific role:** Independently held-out controllability, causal-ablation, numerical-validity, and refinement-equivalence gate.  
**RL status:** This protocol performs no reinforcement learning and cannot establish learned-policy improvement.

**Independent audit:** Read-only standard-library notebook object `4282786714316185` is at `/Workspace/Users/austin.choi@databricks.com/codex_hydrogym_gate0_v3/re100_v3_terminal_audit`; persistent CPU-serverless Job `372974263929719`, run `933651969763400`, task `1023830567796956` completed `SUCCESS` on attempt zero. Local and exported notebook bytes both have SHA-256 `8bb4d76a7bb52bf961e53169c30f9386e530ea77e395a49e2d5d3c71547e9d19`. It independently reproduced 360 traces, 720 windows, 2,520 numerical gates, 60 primary gates, all analysis arithmetic, and exactly one false predicate with zero CFD/RL. It also proved the wrapper root cause: `result.json` was hashed while 15 seed-cluster maps had integer keys, then JSON converted them to strings and changed sorted-key order. Its raw SHA-256 is valid and the typed pre-serialization digest reproduces `97f7002b…`, but its ordinary post-JSON canonical digest is `114d8589…`; the production runner's immediate round-trip validation therefore failed before `air_run_summary.json` and final MLflow upload.

Immutable protocol identity:

- study fingerprint: `885ff77559dadd18cc54d91a30ecb6a48477a4c2baed46fc728635ea3eae8b38`;
- implementation digest: `17fd18a51e8bfb2e8b6d018e7fe824a9b68921fe38d62d231e6634d6203b9dfe`;
- protocol artifact digest: `024039795a851caa0a1ea77580983aa2c869d40d05564f0765fdc56f1920db3f`;
- local artifact: `codex_hydrogym/evidence/gate0_v3/885ff77559da-17fd18a51e8b/protocol.json`;
- Databricks artifact: `/Workspace/Users/austin.choi@databricks.com/codex_hydrogym_gate0_v3/evidence/885ff77559da-17fd18a51e8b/protocol.json`.

## Decision justified by the completed replication

The separately frozen ten-seed Databricks replication completed all 120 planned trajectories and passed every frozen screening predicate. Its result artifact digest is `c783ea92679ad9c3d51fc44a612d15f9c2fa4b548c0dd4d1d99133ce3222e35a`. This positive development-only screen supports designing this gate; it is not pooled into this gate and is not itself a Gate 0 pass.

The v3 design also binds, without reinterpreting:

- the failed Re=100 v2 final report `e8a19fa835a991f233ed7087c31a7c947c6a61a9a3c04ceb935db2870f8dd463`;
- the v2 development-lock artifact `4a71309d5362914856aaceb6009117cba1b01059f9266d95f83d44dfc1041eaf` and lock digest `90c324567e20ab28cec778ad5d5dd53734619f2e04496046d9208e9c229e24df`;
- the negative four-seed diagnostic result `e45eb7f6b19b52d6580462ef22e0efcc87a6cd435916f88694ef25e375777d9b`;
- the positive ten-seed replication study fingerprint `269507101a5206fccab3c90504f7a46009f28381070a0d97875a06429fb19b62`.

No observation from those studies enters the v3 analysis.

## Fixed held-out sample

The gate uses 12 independent held-out seed clusters and one opposite-phase pair:

- reserved seeds: `907, 1009`;
- ten additional prospectively derived seeds: `23642790, 909551482, 680171583, 492410189, 1839799188, 1654216587, 553944181, 2146419921, 164508189, 743151186`;
- held-out phases: `0.1875, 0.6875` turns.

The ten additional seeds are the first valid values generated by the following immutable rule: for counters starting at zero, compute SHA-256 of `codex_hydrogym:re100_gate0_v3:heldout_seed:v1:<counter>`, interpret the first eight digest bytes as an unsigned big-endian integer, reduce modulo `2^31-1`, and accept a nonzero value only when it is distinct and absent from every v1, v2, four-seed diagnostic, ten-seed replication, and reserved seed set. Stop after ten acceptances.

The held-out phases and all 12 seeds must remain unopened until the executable protocol, source manifest, notebook, backend, and review attestation are frozen together. There is no seed replacement, interim decision look, optional stopping, or post-result extension.

## Fixed controller and physics contract

- Reynolds number `100`, JAX float64 required.
- Base: `64×64`, `dt=0.002`.
- Temporal refinement: `64×64`, `dt=0.001`.
- Spatial refinement: `96×96`, `dt=0.002`.
- Uncontrolled burn-in: `100` action intervals.
- Controller warmup: `50` intervals.
- Scoring: two consecutive windows of `100` intervals each.
- Radial action bound: `0.5`.
- Signed feedback: frozen gain `2.0`.
- Fixed observation-free control: `(0.1767766952966369, 0.17677669529663687, 0.0, 0.0)`.
- Privileged oracle: the existing phase-aware radial-bound cancellation controller.
- Observation derangement: the existing one-to-one whole-trajectory permutation that mismatches both phase and seed while preserving global input/action marginals, time structure, and effort.
- No controller tuning or development search occurs in v3.

The five arms are `zero`, `fixed`, `oracle`, `signed_feedback`, and `observation_deranged`. All arms start from byte-identical developed states within each condition/case. Each condition contains `12 seeds × 2 phases × 5 arms = 120` trajectories; the full gate contains exactly `360` decision-bearing trajectories.

The frozen execution backend is one Databricks AI Runtime `GPU_1xH100`, matching the user's explicit preference for GPU capacity when faster. The protocol must not pool CPU, local, prior, or retry observations. A backend change requires a new pre-execution protocol version.

## Frozen base-condition gates

Every predicate below is conjunctive; one false value fails the gate.

1. All seven numerical gates pass for every trace.
2. All 20 existing v2 primary causal, pairing, marginal, effort, materiality, opposite-phase, and independent-seed predicates pass.
3. Signed feedback beats zero in every seed × phase × window block.
4. For feedback versus each of zero, fixed, and observation-deranged control:
   - both aggregate scoring-window effects are at least `5%`;
   - the seed-clustered two-sided 95% t-interval lower bound is at least `5%`.
5. Signed-feedback mean RMS-L2 effort does not exceed privileged-oracle effort.
6. Initial-state, control-start, window-continuity, observation-history, action-history, and numerical-gate identities reproduce from the persisted artifacts.

The seed cluster—not phase or window—is the independent inference unit. Phase and window observations are averaged within seed before a t-interval is formed. The 95% t critical for 12 clusters (`df=11`) is frozen at `2.200985160082949`.

## Frozen refinement gates

The base, temporal, and spatial conditions use identical held-out cases, arms, horizons, and analysis code. Each refinement must satisfy all base-condition causal and numerical predicates, then all of the following:

- temporal maximum relative arm-mean difference across all five arms ≤ `2%`;
- spatial maximum relative arm-mean difference across all five arms ≤ `5%`;
- temporal absolute aggregate-effect difference across all five existing effect pairs ≤ `2` percentage points;
- spatial absolute aggregate-effect difference across all five existing effect pairs ≤ `3` percentage points;
- for every effect pair, the paired-seed two-sided 90% t-interval for refined-minus-base effect lies wholly inside `[-2,+2]` percentage points temporally or `[-3,+3]` spatially;
- exact five-arm TKE ordering is preserved;
- the complete materiality/opposite-pair/effort decision vector is preserved;
- all executed target and derangement-source traces pass all numerical gates.

The five effect pairs are oracle-versus-zero, oracle-versus-fixed, feedback-versus-zero, feedback-versus-fixed, and feedback-versus-deranged. The 90% t critical for 12 clusters (`df=11`) is frozen at `1.7958848187036691`. The intervals are equivalence screens (TOST-compatible 90% intervals), and all screens must pass; no multiplicity-based predicate dropping is permitted.

## Sample-size rationale

The ten-seed replication estimated paired refined-minus-base effect standard deviations of approximately `0.0210430` temporally and `0.0193159` spatially. At 12 seed clusters, the plug-in 90% half-widths are approximately `0.0109093` and `0.0100139`. Keeping the observed replication means only as planning inputs projects intervals `[-0.00880, 0.01302]` and `[-0.00922, 0.01081]`, inside the unchanged `±0.02` and `±0.03` margins. This is a prospective sizing justification, not a promise that v3 will pass. The larger sample also provides two additional clusters beyond the development replication while keeping the 360-trajectory gate bounded.

## Artifact and execution readiness

Completed without opening a reserved case:

1. new v3 schema and runner `codex_hydrogym/gate0/re100_v3.py`;
2. content-addressed protocol, implementation manifest, controller lock, evidence namespace, immutable per-condition writers, strict loaders, and conjunctive analysis;
3. zero-CFD Databricks notebook object `838420173929124`, Job/run/task `693336960365518` / `132154788305477` / `336953652096218`, which passed all 23 protocol, source, fail-closed, and synthetic pass/fail checks;
4. wheel SHA-256 `e381b42d415b0644fd773be67ef9aab94133289e6559933bf72e079da50e2e51` and workspace file object `838420173929123`;
5. v3-specific one-H100 AI Runtime preflight Job/run/task `1057054860106994` / `199802627560370` / `751301736884830`, which completed `SUCCESS` after a capacity-delayed startup. MLflow run `3b16e54b500d49d6a36866d0343ce386` ended `FINISHED` with `gate0_v3/preflight.json` and the frozen protocol. The canonical payload proves Python 3.12, JAX/JAXLIB `0.7.2`, float64, GPU backend, one `NVIDIA H100 80GB HBM3`, the exact wheel/protocol/implementation digests, zero prior/local observations, `cfds_executed=0`, `execution_authorized=false`, `reserved_cases_opened=false`, and `rl_training_performed=false`;
6. a post-preflight workspace audit showing that the v3 evidence namespace still contains only the frozen `protocol.json`, with no condition or result artifact.

After explicit user approval, authorization preflight Job/run/task `236084355460379` / `251966954943330` / `954992926132786` completed `SUCCESS` on attempt zero. MLflow run `898748e8ef17461399f37ef746208541` ended `FINISHED` and its canonical payload confirms the exact review attestation and secret-backed token, one H100, JAX x64, all bound hashes, no prior/local observations, zero CFD, no opened reserved case, and no RL. A post-run namespace audit again found only `protocol.json`.

The final namespace audit immediately before launch again matched exactly the sole frozen `protocol.json`. Primary Job/run/task `98243916406855` / `425683771687715` / `429856525625340` was submitted once with digest-bound idempotency key `f0ffd5678ec43bb66e3bba03bcefae19043810eb24aa0bb97a3aac9e78fc2afb`; it is running on one H100 with no retries. MLflow run `e7caeb85879c4aa988c5c39d05ed781d` is bound to this execution. Only terminal artifacts may be opened for analysis.

The primary MLflow claim-role/backend/study/workflow tags appeared after approximately 46 minutes of capacity provisioning. Because the entry point sets them only after `_validate()` returns, the external authorization, exact source and artifact identities, one-H100/JAX-x64 runtime, and protocol-only namespace checks passed before any trajectory. No partial log, condition artifact, or metric was inspected.

The primary wrote all three condition artifacts and `result.json` before the outer AIR task ended `FAILED`/`INTERNAL_ERROR`. The result records all 360 trajectories, all 720 windows, artifact digest `97f7002bf33e87beb02eef1a8f27f0ce73139641a6867f02cb61205ce67b9636`, and `passed=false`. Exactly one frozen predicate failed: the temporal feedback-versus-zero paired-seed 90% refined-minus-base effect interval was `[-0.020255923821008402, 0.004795061435287584]`, whose lower endpoint is `0.000255923821008402` outside the `-0.02` margin. All other frozen predicates passed. The outer wrapper failed before writing `air_run_summary.json` or uploading final MLflow artifacts; this does not alter the immutable scientific failure. No retry, sample extension, threshold change, or PPO run is allowed.

The first preflight attempt, Job/run/task `316596818779054` / `403428098499749` / `108711422687827`, timed out after 60 minutes before its entry point started. Its MLflow run `06f57e430b3a435a883861eab9bc23ca` ended `FAILED` without custom tags or artifacts. The successful retry changed only the control-plane timeout to 120 minutes; the entry point, scientific inputs, hashes, H100 count, and zero-CFD boundary were unchanged. Entry-point SHA-256 is `0cd4f68cd6501c95b76f9fb204747f194eeebb8aa55606b87f08b9a67f5d68f6`; base workload SHA-256 is `2d263081c398fa1132e5a439a2b3cfc21cf23c65016c098279669389a616b706`.

The following authorization requirements are complete before any held-out trajectory runs:

1. an explicit digest-bound human review attestation and execution token separate from the protocol artifact;
2. a final read-only audit immediately before execution confirming that the execution namespace still contains the frozen protocol and no condition/result artifact;
3. explicit user approval to perform the one decision-bearing held-out execution.

The protocol itself remains immutable and records `execution_authorized=false`, `reserved_cases_opened=false`, and `rl_training_performed=false`; authorization is deliberately external. The runner refuses `--stage run` without the exact protocol plus the digest-bound review attestation and matching external token. Those external requirements and explicit user approval are now complete. A pass would authorize repairing and evaluating the PPO task contract; it would still not be evidence that RL improves the fluid. A failure is terminal for this v3 formulation.
